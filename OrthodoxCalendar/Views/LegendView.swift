import SwiftUI

// Legend is no longer used in list view layout.
// Kept as an empty placeholder to avoid dangling references.
// Can be removed once all references are cleaned up.
struct LegendView: View {
    var body: some View {
        EmptyView()
    }
}
